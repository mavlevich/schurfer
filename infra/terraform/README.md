# Terraform

Status: inactive scaffold. No production infrastructure is currently managed from
this directory.

Production runs on a Hetzner host in Nuremberg and is operated through Docker Compose
and the [production runbook](../../docs/runbooks/README.md). Do not infer region,
capacity, or reproducibility from this empty Terraform directory. Infrastructure as
code requires a separate reviewed decision after the measured hosting boundary is
known.
