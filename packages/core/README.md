# core

Status: inactive scaffold.

Originally planned for shared types, models, errors, and cross-language structures.

No code imports this directory today, so it is not a source of truth. Active contracts
live in versioned schemas, migrations, service-local types, and `docs/contracts`.
